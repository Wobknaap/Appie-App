name: AH Koopjes Collector

on:
  schedule:
    - cron: "*/5 * * * *"
  # workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Debug trigger
        run: |
          echo "event=${{ github.event_name }}"
          date -u
